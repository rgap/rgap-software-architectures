/**
 * Shared domain shape for a product.
 * In TypeScript this would be an interface; in JS we document by convention.
 *
 * @typedef {{ id: string, name: string, category: string, price: number }} Product
 */
